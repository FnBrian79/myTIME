<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mytime.dojo">

    <!-- Network: Required for ElevenLabs API, WebSocket bridge, and SIP -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Audio: Required for voice capture and playback -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- SIP/VOIP: Critical for myTIME Combat Ring -->
    <uses-permission android:name="android.permission.USE_SIP" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <!-- Foreground service: Keep Combat Ring alive during calls -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Vibration: Combat Ring ringtone feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- SIP hardware feature declaration -->
    <uses-feature android:name="android.hardware.sip.voip" android:required="true" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="myTIME"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Main React Native Activity -->
        <activity
            android:name=".MainActivity"
            android:label="myTIME"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SIP Incoming Call Receiver -->
        <receiver
            android:name=".SipCallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.sip.action.SIP_INCOMING_CALL" />
            </intent-filter>
        </receiver>

        <!-- Combat Ring Foreground Service -->
        <service
            android:name=".CombatRingService"
            android:foregroundServiceType="phoneCall"
            android:exported="false" />

    </application>
</manifest>
