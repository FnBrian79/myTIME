# myTIME .gitignore
# Ignore logs and secrets, but keep configurations

# Database & Vault
learning_repo/*.db
learning_repo/forensic_ledger.jsonl
learning_repo/keys/

# Logs
logs/*.log

# Foreman Private Logic (ensure triage doesn't leak info)
# services/foreman/triage.py  <-- Optional: Only if you want to keep the logic private. 
# For now, we keep the code but ignore any data it generates.

# Docker
.DS_Store
*.swp
__pycache__/
*.pyc

# ElevenLabs secrets (NEVER commit real keys)
.env
services/bridge/.env
*.elevenlabs

# React Native / Mobile
mobile/node_modules/
mobile/android/.gradle/
mobile/android/app/build/
mobile/android/build/
mobile/android/local.properties
mobile/ios/Pods/
mobile/.metro-health-check*
mobile/.bundle/
